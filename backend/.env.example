# Use as mesmas credenciais do docker-compose quando rodar o backend localmente
# contra o Postgres no Docker (docker compose up -d postgres).
DB_HOST=localhost
DB_PORT=5432
DB_USER=cflow
DB_PASSWORD=cflow_secret
DB_NAME=cflow

FRONTEND_URL=http://localhost:4200
CORS_ORIGINS=*
